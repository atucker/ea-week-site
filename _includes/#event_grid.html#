<hr class="featurette-divider" />

<center>
  <div class="container">
    <div class="row-fluid">
      {% for slug in include.data %}
      {% assign event = site.events[slug]%}
      <div class="col-lg-4">
        <a href="{{event.url}}" class="nounderline">
	  <img class="img" src="{{event.poster}}" width="65%"></br />
        
          <!--<img class="img-circle" src="{{event.image}}" width="210" height="210">-->
          <h1 class="text-primary"><strong>{{event.title}}</strong></h1>
          <h2 class="text-primary">{{event.speaker}}</h2></a>
          {% include spacetime.html data=event.spacetime %}
	<a href="{{event.facebook}}" >RSVP</a></br />
        <a href="{{event.url}}">&gt; see more &lt;</a>
        <h2><small>{{event.sub}}</small></h2>
      </div>
      {% endfor %}
    </div>
  </div>
</center>
